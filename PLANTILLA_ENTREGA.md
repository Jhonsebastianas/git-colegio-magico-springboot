# 📝 Plantilla de Entrega: El Pensadero

Responde a las siguientes preguntas como parte de tu evaluación final. Guarda una copia de este archivo con tu nombre (ej: `JUAN_PEREZ.md`).

1.  **¿Usaste Inteligencia Artificial para resolver los retos (del 1 al 4)?**
    *Respuesta:* Sí, usé IA (Claude) como apoyo durante el desarrollo de los 4 retos.

2.  **¿Para qué específicamente te fue útil la IA? (Ej. para entender la lógica de las anotaciones, para explicar los errores de Java, para que hiciera el código desde cero)**
    *Respuesta:* La usé principalmente para entender la lógica detrás de cada anotación (por ejemplo, por qué `@Id` y `@GeneratedValue` van juntos, o la diferencia entre `createQuery` con JPQL y `createNativeQuery` con SQL), para que me explicara qué validaba exactamente cada test antes de escribir el código, y para revisar mi código una vez que yo ya lo había escrito y confirmar si cumplía lo pedido. Yo no escribí el código desde 0, la IA me daba una plantilla y yo la rellenaba para entender mas o menos como funcionaba la lógica.

3.  **¿Qué fue lo que más se te dificultó al trabajar con la Arquitectura por Capas en Spring Boot?**
    *Respuesta:* (Se me dificultó mucho ABSOLUTAMENTE TODO, a pesar de que le entendí el 90% de las cosas a el teacher Sebas, al realizar el taller me quedaba en blanco ya que es mi primera vez haciendo esto)

4.  **¿Qué aprendiste sobre la creación de APIs REST y el uso de Jakarta en esta academia?**
    *Respuesta:* (Aprendí que con Jakarta se usan anotaciones como `@GET` y `@POST` para decirle a la API qué hacer según la petición, y que hay que devolver un código como 201 si todo sale bien o 400 si hay un error.)